# {{ project_name }}

legoback으로 생성된 프로젝트입니다.

포함 모듈: {% if modules %}{{ modules | join(', ') }}{% else %}(없음 — 최소 뼈대){% endif %}

## 실행

```bash
cd {{ project_name }}
.venv\Scripts\activate
uvicorn src.main:app --reload      # 로컬 실행
# 또는 docker 모듈을 선택했다면:
docker compose up
```

동작 확인: http://localhost:8000/health
API 문서: http://localhost:8000/docs

{% if 'docker' in modules and 'database' in modules %}

## Docker로 실행하기

DB 관련 명령어는 프로젝트 생성 시 고른 db_type(PostgreSQL/MySQL/MongoDB)에 맞는 것만
골라서 실행하세요.

### 1. Docker Desktop이 켜져 있는지 확인

```bash
docker ps
```

### 2. 컨테이너 빌드 및 실행

```bash
cd {{ project_name }}
docker compose up --build -d
```
{% if 'database' in modules %}

### 3. 데이터베이스 준비

**PostgreSQL / MySQL을 선택했다면** — 모델을 처음 쓸 때 한 번, 테이블을 만드는
마이그레이션이 필요합니다:
```bash
docker compose exec app alembic revision --autogenerate -m "create users table"
docker compose restart app
```
서버가 재시작되는 순간, 방금 만든 마이그레이션 파일이 자동으로 DB에 적용됩니다
(`alembic upgrade head`를 따로 안 쳐도 됩니다).

**MongoDB를 선택했다면** — 이 단계는 필요 없습니다. MongoDB는 컬렉션이 데이터를
처음 넣을 때 자동으로 생기므로, 마이그레이션 없이 바로 다음 단계로 넘어가면 됩니다.

### 4. 데이터 직접 확인하기

아래 명령어는 이후 단계(회원 목록/역할 확인)에서도 그대로 재사용합니다.

**PostgreSQL:**
```bash
docker compose exec db psql -U app -d app -c "SELECT email, role FROM users;"
```

**MySQL:**
```bash
docker compose exec db-mysql mysql -uapp -papp app -e "SELECT email, role FROM users;"
```

**MongoDB:**
```bash
docker compose exec mongo mongosh -u app -p app --authenticationDatabase admin app --eval "db.users.find({}, {email:1, role:1, _id:0}).toArray()"
```
{% endif %}

### 5. API 테스트 — http://localhost:8000/docs

Swagger 화면에서 각 엔드포인트의 "Try it out" 버튼을 눌러 테스트합니다.
{% if 'jwt-auth' in modules %}

1. `POST /auth/signup` — 회원가입 (첫 번째로 가입한 사람은 자동으로 `ADMIN`)
2. `POST /auth/signup` — 다른 email로 한 번 더 (두 번째 사람은 `USER`)
3. 위 4번의 명령어로 role 확인 (첫 번째는 `ADMIN`, 두 번째는 `USER`)
{% endif %}
{% if 'rbac' in modules %}4. 우측 상단 **Authorize** 버튼 클릭 → `POST /auth/login`에서 첫 번째로 가입한 사람의
email과 password 넣어서 `access_token` 붙여넣기
5. `PATCH /admin/users/{email}/promote` — 두 번째 사람 이메일로 실행 → 200, role이 `ADMIN`으로
6. `PATCH /admin/users/{email}/demote` — 방금 그 사람 다시 강등 → 200, role이 `USER`로
7. 마지막 남은 ADMIN을 demote 시도 → **409** (마지막 관리자 보호)
8. 없는 이메일로 promote 시도 → **404**
9. Authorize를 풀고(또는 USER 토큰으로) promote 시도 → **403**
{% endif %}
{% if 'jwt-auth' in modules %}10. `POST /auth/login` — 로그인 확인
11. `POST /auth/refresh` — refresh_token으로 새 access_token 발급 확인
{% endif %}

### 6. 정리

```bash
docker compose down -v
```
{% endif %}
{% if 'database' in modules %}

## DB 테이블 관리 (Alembic)

이 프로젝트는 파이썬 코드로 적어둔 모델(예: `src/models/user.py`의 `User` 클래스)을 보고
실제 데이터베이스에 테이블을 만들어주는 **Alembic**이라는 도구를 쓰고 있습니다.

### 처음 실행할 때 (테이블이 하나도 없는 상태)

1. 아래 명령어로, "지금 코드에 있는 모델대로 테이블을 만들어라"는 내용이 적힌 파일을 하나 만듭니다.
   ```bash
   alembic revision --autogenerate -m "create users table"
   ```
   - `-m` 뒤의 문구는 진짜 명령어가 아니라 그냥 이 변경에 붙이는 이름표예요 (아래 설명 참고).
   - 이 명령어는 아직 DB를 건드리지 않아요. `migrations/versions/` 폴더 안에 새 파일이
     하나 생겼는지만 확인하면 됩니다.
2. 방금 생긴 그 파일을 한 번 열어서 내용을 훑어봅니다 (왜 확인해야 하는지는 맨 아래 참고).
3. 서버를 켭니다.
   ```bash
   uvicorn src.main:app --reload
   # 또는 docker 모듈을 선택했다면:
   docker compose up
   ```
   **서버가 켜지는 순간, 방금 만든 파일이 자동으로 DB에 적용돼서 실제 테이블이 생깁니다.**
   예전엔 `alembic upgrade head`라는 명령어를 따로 쳐야 했지만, 지금은 서버 시작 코드에
   이미 포함돼 있어서 따로 안 쳐도 됩니다.

### 나중에 모델을 추가하거나 수정했을 때

모델을 바꿀 때마다(테이블 추가, 필드 추가/삭제 등) 위 1~3번을 그대로 반복하면 됩니다.
모델을 하나도 안 바꿨다면 이 과정을 할 필요가 전혀 없습니다.

### `-m` 뒤에는 뭘 적어야 하나요?

Alembic이 그 글자 내용을 읽고 뭔가 다르게 동작하는 게 아니에요. 나중에 `migrations/versions/`
폴더에 파일이 여러 개 쌓였을 때, "이게 그때 그 변경이었지" 하고 사람이 알아보기 위한
메모일 뿐이라 정해진 답이 없습니다. 지금 뭘 바꿨는지 짧게 적으면 됩니다. 예:

- `alembic revision --autogenerate -m "create users table"` — 회원 테이블을 처음 만들 때
- `alembic revision --autogenerate -m "add nickname to users"` — User 모델에 닉네임 필드를 추가했을 때
- `alembic revision --autogenerate -m "create posts table"` — 게시글 모델을 새로 추가했을 때

### 생성된 마이그레이션 파일, 왜 확인해야 하나요?

Alembic의 자동 감지가 항상 완벽하지는 않습니다. 예를 들어 필드 이름만 바꾼 것
(`hashed_password` → `password_hash`)을 Alembic이 "기존 필드 삭제 + 새 필드 추가"로
잘못 인식할 수 있어요. 이걸 그대로 적용하면 기존 데이터가 사라질 수 있습니다. 그래서
`migrations/versions/` 안에 새로 생긴 파일은 커밋하기 전에 한 번 내용을 읽어보는 걸
권장합니다.
{% endif %}

