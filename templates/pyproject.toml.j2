[project]
name = "{{ project_name }}"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
{% for pkg in packages %}
    "{{ pkg }}",
{% endfor %}
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "httpx>=0.27"]
